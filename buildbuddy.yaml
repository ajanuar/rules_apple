actions:
  - name: "Test all targets"
    triggers:
      push:
        branches:
          - "test-build"
      pull_request:
        branches:
          - "test-build"
    bazel_commands:
      - "bazel run @build_bazel_rules_swift//tools/dump_toolchains"
      
